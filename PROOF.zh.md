# Erdős 1027 / JSP-000854：一个初等概率证明

研究日期：2026-09-16。

**成果范围：**本文给出覆盖原题全部量词的数学证明，并给出显式常数。它不是新的问题解决声明：原问题已有 Koishi Chan 的公开证明。本次重建的路线只使用随机部分染色、并集界、Markov 不等式及基本指数估计。未主张该路线的首次发现或获奖资格。

**机器验证范围：**原题完整的定性结论已经在 Lean 4.19.0 / Mathlib v4.19.0 中形式化并实际编译通过。最终定理为 `Erdos1027Main.erdos_1027_subsets`（另有等价的二染色版本）。有限乘积概率、三类坏事件、补全计数、均匀分布与子集计数转换均已接通。传递公理审计只出现 `propext`、`Classical.choice`、`Quot.sound`，没有 `sorryAx` 或自定义解答公理。Lean 还验证了给定数值阈值条件下的定量下界；下文特定的取整公式和小数示例尚未单独封装为 Lean 推论。

## 1. 原题与显式结论

给定任意实数 \(c>0\)。当正整数 \(n\) 足够大时，设 \(\mathcal F\) 是有限集合族，每个成员恰有 \(n\) 个元素，并且

\[
m:=|\mathcal F|\le c2^n,\qquad X:=\bigcup_{A\in\mathcal F}A.
\]

称 \(B\subseteq X\) 合法，如果对每个 \(A\in\mathcal F\)，都有

\[
A\cap B\ne\varnothing,\qquad A\setminus B\ne\varnothing.
\]

换言之，把 \(B\) 染红、其余元素染蓝，每个集合都必须同时看到红色和蓝色。原题要求证明：合法 \(B\) 的数量至少是 \(\delta(c)2^{|X|}\)，其中 \(\delta(c)>0\) 与 \(n\)、\(X\)、\(\mathcal F\) 无关。

**本文证明如下显式版本。**令

\[
k=\log(8(c+1)),\qquad L=\left\lceil64c(c+1)\right\rceil,
\]
\[
N(c)=\left\lceil\max\{2k,\ 1024c^2(c+1)^2k\}\right\rceil,
\qquad \delta(c)=2^{-L-2}.
\]

这里 \(\log\) 是自然对数。对所有整数 \(n\ge N(c)\)，都有

\[
\boxed{\#\{B\subseteq X:B\text{ 合法}\}\ \ge\ 2^{-L-2}\,2^{|X|}.}
\]

这些常数非常保守；本次不研究最优常数。若 \(\mathcal F=\varnothing\)，则 \(X=\varnothing\)，唯一的空集合法，结论直接成立。

## 2. 先留下一些未染色元素

设

\[
p=\frac{k}{n},\qquad b=\frac{1-p}{2},\qquad a=\frac{1+p}{2}.
\]

由 \(n\ge2k>0\)，有 \(0<p\le1/2\)。独立地给每个元素分配以下三种状态：

| 状态 | 概率 | 含义 |
|---|---:|---|
| 固定红色 | \(b\) | 后续保持红色 |
| 固定蓝色 | \(b\) | 后续保持蓝色 |
| 灰色 | \(p\) | 后续再独立公平地选红或蓝 |

若某个集合 \(A\) 没有固定红点，称它产生一个“缺红要求”；没有固定蓝点则产生一个“缺蓝要求”。让 \(M\) 表示这两类要求的总数。一条全灰集合会产生两个要求，必须算两次。

目标是找到概率至少 \(1/4\) 的部分染色，使得这些要求可以通过指定至多 \(L\) 个灰点的颜色全部满足。

## 3. 第一类坏事件：要求已经无法补救

缺红要求若没有灰点，就意味着整条集合已经固定为蓝色，无法补救；缺蓝同理。

设 \(E_0\) 是存在全固定红或全固定蓝集合的事件。并集界给出

\[
\Pr(E_0)\le2mb^n
\le2c(1-p)^n
\le2ce^{-np}
=2ce^{-k}
=\frac{c}{4(c+1)}<\frac14.
\]

所以在 \(E_0\) 不发生时，每个缺色要求至少有一个灰点可以使用。

## 4. 第二类坏事件：要求太多

一个指定集合没有固定红点的概率是 \((b+p)^n=a^n\)，缺蓝同理。因此由期望的线性性，

\[
\mathbb E[M]=2ma^n
\le2c(1+p)^n
\le2ce^{np}
=2ce^k
=16c(c+1).
\]

不需要假设不同集合之间独立。由 \(L\ge64c(c+1)>0\) 和 Markov 不等式，

\[
\Pr(M\ge L)\le\frac{\mathbb E[M]}{L}\le\frac14.
\]

## 5. 第三类坏事件：相反要求相互冲突

考虑有向集合对 \((A,B)\)，其中 \(A\cap B\ne\varnothing\)。设交集大小为 \(\ell\ge1\)。

若 \(A\) 缺红而 \(B\) 缺蓝，那么交集中的每个元素既不能固定为红、也不能固定为蓝，所以必须全为灰色。其余 \(2(n-\ell)\) 个元素各自只需避开一种固定颜色。由元素状态的独立性，

\[
\Pr(A\text{ 缺红且 }B\text{ 缺蓝})
=p^\ell a^{2(n-\ell)}.
\]

又因为

\[
a^2-p=\frac{(1-p)^2}{4}\ge0,
\]

所以

\[
p^\ell a^{2(n-\ell)}
=p\,p^{\ell-1}a^{2(n-\ell)}
\le p\,a^{2(\ell-1)}a^{2(n-\ell)}
=p\,a^{2n-2}.
\]

令 \(E_\times\) 表示存在相交的“缺红集合、缺蓝集合”有向对。**这里必须包括 \(A=B\)**，因此全灰集合也属于冲突事件，不能漏算。

这样的有向对至多有 \(m^2\) 个。由于 \(a\ge1/2\)，

\[
\begin{aligned}
\Pr(E_\times)
&\le m^2p\,a^{2n-2}\\
&\le4m^2p\,a^{2n}\\
&\le4c^2p(1+p)^{2n}\\
&\le4c^2\frac{k}{n}e^{2k}\\
&=\frac{256c^2(c+1)^2k}{n}
\le\frac14.
\end{aligned}
\]

最后一步正是 \(n\ge1024c^2(c+1)^2k\)。以上只是对各事件使用并集界，不曾假设它们相互独立。

## 6. 好的部分染色有正概率

由三次坏事件的概率界，

\[
\Pr\big(E_0^c\cap\{M<L\}\cap E_\times^c\big)
\ge1-\Pr(E_0)-\Pr(M\ge L)-\Pr(E_\times)
\ge\frac14.
\]

称满足这三项条件的部分染色为“好部分染色”。固定这样一个部分染色。

对每个缺红要求，从对应集合中任选一个灰点，规定它最终染红；每个缺蓝要求也任选一个灰点，规定它最终染蓝。

这些规定不会矛盾：如果一个点同时被要求染红和染蓝，它就属于一个缺红集合和一个缺蓝集合的交集，与 \(E_\times^c\) 矛盾。同色要求可以共用同一点，不会造成问题。

总共被规定颜色的不同灰点数 \(r\) 至多是要求数：

\[
r\le M\le L.
\]

给其余灰点任意染色，就得到了合法二染色：原本已有的固定颜色不会消失，每一种缺少的颜色又有选定代表提供。

因此，若将所有灰点独立公平补全，满足这些规定的概率是 \(2^{-r}\)，至少为

\[
2^{-L}.
\]

**注意这里得到的是大量合法补全，而不只是一个补全。** 若灰点共有 \(g\) 个，则至少有 \(2^{g-r}\) 个合法补全。

## 7. 从随机补全回到原题的计数

完整的随机过程是：先独立三色分配，再对每个灰点独立抛公平硬币选红或蓝。

任一元素最终为红的概率为

\[
\frac{1-p}{2}+p\cdot\frac12=\frac12,
\]

最终为蓝也一样。不同元素用到的随机变量彼此独立，因此最终结果恰好在全部 \(2^{|X|}\) 个二染色中均匀分布。

利用好部分染色及其合法补全比例，

\[
\Pr(\text{最终合法})
\ge\Pr(\text{好部分染色})\,2^{-L}
\ge2^{-L-2}.
\]

于是合法红色集合 \(B\) 的个数至少为 \(2^{-L-2}2^{|X|}\)。这就证明了原题，而且 \(L,N,\delta\) 全都只依赖 \(c\)。证毕。

## 8. 一个数值例子与验证边界

当 \(c=1\) 时，这份证明可取

\[
L=128,\quad N=\lceil4096\log16\rceil=11357,\quad
\delta=2^{-130}\approx7.35\times10^{-40}.
\]

这个比例极小，但严格为正，且不随顶点数或集合大小下降。它足以回答原题的定性问题；它不是实际合法染色比例的预测，也没有声称阈值最优。

附带的有限穷举程序用于检查代表选择、交集冲突和随机补全的计数关系。有限实验不能替代上述对任意 \(c,n,\mathcal F\) 的证明。

## 9. 来源、归属与形式化结果

- [原题](https://www.erdosproblems.com/1027)；[LaTeX 题面](https://www.erdosproblems.com/latex/1027)。
- [Koishi Chan 的原证明及修订讨论](https://www.erdosproblems.com/forum/thread/1027)。其公开路线使用非均匀 Property B 定理与自适应随机染色；本笔记没有复现该路线。
- [JSP-000854 题库记录](https://github.com/TheJustinSunPrize/awards/blob/main/problems/catalog-0801-0900.md#JSP-000854)。
- [teorth/erdosproblems](https://github.com/teorth/erdosproblems) 的下载数据仍标为未形式化；这只是所查资料的状态，并非穷尽性的新颖性保证。
- 查到的 [LeanGenius 完整题目文件](https://github.com/rjwalters/lean-genius/blob/main/proofs/Proofs/Erdos1027Problem.lean) 把完整解答声明成 `axiom erdos_1027_solution`。这不能作为完整 Lean 证明使用。本项目没有导入这一公理，也没有复制该项目源码。

上述连接已全部完成。`Erdos1027Main.erdos_1027_subsets` 对任意 `c > 0`，先给出正比例 `δ` 和自然数阈值 `N`，再对任意有限顶点集、任意 `n ≥ N` 及满足规模条件的集合族证明合法子集计数下界。可复现命令、模块说明和公理审计记录见代码包中的 `README.md`、`LEAN-STATUS.md` 与 `verification/axioms.log`。本次结果不构成首次形式化或奖项资格声明。
